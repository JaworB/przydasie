# Laptop-specific bindings
# These bindings are for laptop use with DisplayLink dock

bind = SUPER, M, exec, ~/.local/bin/toggle-dock-mode.sh
bind = SUPER SHIFT, V, exec, ~/.local/bin/fix-cursor-vertical.sh
bind = SUPER SHIFT, A, exec, ~/.local/bin/toggle-audio-output.sh
