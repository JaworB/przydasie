# Kopiuj do local.env (NIEśledzony w git — patrz .gitignore).
# Podsieć LAN używana do tymczasowego dostępu SSH podczas etapu "join"
# (patrz vps-join/join-host.sh). Domyślna wartość w 30-firewall.sh to
# 192.168.0.0/16 — nadpisz, jeśli Twoja domowa sieć jest inna.
JAWOR_LAN_CIDR=192.168.1.0/24
