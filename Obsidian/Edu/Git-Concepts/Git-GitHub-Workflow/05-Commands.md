# Commands

Common GitHub workflow commands.

## Essential Commands

| Action | Command |
|--------|---------|
| Check status | `git status` |
| Add changes | `git add .` |
| Commit | `git commit -m "message"` |
| Push | `git push origin main` |
| Force push | `git push --force origin main` |
| Check remote | `git remote -v` |
| Change remote | `git remote set-url origin new_url` |
| View history | `git log --oneline` |
| Search in history | `git log --all -S "search_term"` |

## Related

- See [[04-Removing-Credentials]] for history cleanup
- See [[06-Warnings]] for cautions
- See [[index]] for overview

## Next Steps

Proceed to [[06-Warnings]] to understand important cautions.
